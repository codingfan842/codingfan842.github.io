function initTippy() { document.querySelectorAll('[data-tippy-root]').forEach(el => { if (el._tippy) { el._tippy.destroy() } }); tippy('.tippy', { onMount(instance) { const box = instance.popper.firstElementChild; requestAnimationFrame(() => { box.classList.add('animation') }) }, onHidden(instance) { const box = instance.popper.firstElementChild; box.classList.add('animation') }, content(reference) { const title = reference.getAttribute('title'); reference.removeAttribute('title'); return title }, allowHTML: !0, followCursor: !0, theme: 'dorian', arrow: !1, duration: 100, }) }initTippy()